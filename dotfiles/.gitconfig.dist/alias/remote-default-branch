!sh -c "git config branch.remote-default-branch || (git config branch.remote-default-branch \\$(git remote show origin | grep 'HEAD branch' | cut -d' ' -f5) && git config branch.remote-default-branch)"
